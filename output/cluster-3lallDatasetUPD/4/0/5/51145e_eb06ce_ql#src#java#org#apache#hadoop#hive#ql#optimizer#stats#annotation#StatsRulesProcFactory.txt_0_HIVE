eb06ce
@@ -1396,3 +1396,3 @@
       List<Integer> result = Lists.newArrayList();
-      if (ops != null || !ops.isEmpty()) {
+      if (ops != null && !ops.isEmpty()) {
         for (int i = 0; i < ops.size(); i++) {
